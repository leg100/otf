{{ template "layout" . }}

{{ define "title" }}Login{{ end }}

{{ define "container" }}
  {{ template "flash" . }}
  <div class="center">
    <div class="login-buttons-container">
      {{ range .Content }}
        <a href="{{ .RequestPath }}">
          {{ template "icons" .CloudName }}<span>Login with {{ title .CloudName }}</span>
        </a>
      {{ end }}
    </div>
  </div>
{{ end }}
