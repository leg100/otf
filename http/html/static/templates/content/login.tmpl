{{ template "layout" . }}

{{ define "title" }}Login{{ end }}

{{ define "container" }}
  {{ template "flash" . }}
  <div class="center">
    {{ range .Content }}
      <a href="{{ .RequestPath }}">
        {{ template "github_icon" }}
        <span>Login with {{ title .CloudName }}</span>
      </a>
    {{ end }}
  </div>
{{ end }}
