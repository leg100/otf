{{ define "page-navigation-links" }}
  <div class="flex p-4 gap-2 justify-end">
    {{ with prevPageQuery . }}
      <a id="prev-page-link" href="">Previous Page</a>
    {{ end }}
    {{ with nextPageQuery . }}
      <a id="next-page-link" href="">Next Page</a>
    {{ end }}
  </div>
{{ end }}
